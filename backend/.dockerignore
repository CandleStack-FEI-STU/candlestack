# Allowlist: the build context is pyproject.toml, uv.lock, src/ and granian-logging.json, all
# that any stage (dev included) needs.
*
!pyproject.toml
!uv.lock
!src
!granian-logging.json
**/__pycache__
**/*.pyc
